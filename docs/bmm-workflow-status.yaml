# BMM Workflow Status
# Tracks progress through BMad Method phases (Discovery, Planning, Solutioning)
# Implementation phase tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-12-03"
project: "unitree-g1-robot"
project_type: "Construction site inspection robot using Unitree G1 EDU"
selected_track: "method"
field_type: "greenfield"
workflow_path: "method-greenfield.yaml"

workflow_status:
  phase_0_discovery:
    name: "Discovery"
    workflows:
      - id: "brainstorm-project"
        status: "skipped"
        agent: "analyst"
        command: "/bmad:bmm:agents:analyst then brainstorm-project"

      - id: "research"
        status: "skipped"
        agent: "analyst"
        command: "/bmad:bmm:agents:analyst then research"

      - id: "product-brief"
        status: "docs/analysis/product-brief-unitree-g1-robot-2025-12-03.md"
        agent: "analyst"
        command: "/bmad:bmm:agents:analyst then product-brief"
        completed: "2025-12-03"

  phase_1_planning:
    name: "Planning"
    workflows:
      - id: "prd"
        status: "docs/prd.md"
        agent: "pm"
        command: "/bmad:bmm:agents:pm then prd"
        output: "Product Requirements Document with FRs and NFRs"
        completed: "2025-12-03"

      - id: "validate-prd"
        status: "skipped"
        agent: "pm"
        command: "/bmad:bmm:agents:pm then validate-prd"

      - id: "create-ux-design"
        status: "skipped"
        condition: "if_has_ui"
        agent: "ux-designer"
        command: "/bmad:bmm:agents:ux-designer then create-ux-design"
        note: "Skipped - IoT/Robotics project with CLI interface, no user-facing UI"

  phase_2_solutioning:
    name: "Solutioning"
    workflows:
      - id: "create-architecture"
        status: "docs/architecture.md"
        agent: "architect"
        command: "/bmad:bmm:agents:architect then create-architecture"
        output: "System architecture document"
        completed: "2025-12-04"

      - id: "create-epics-and-stories"
        status: "docs/epics.md"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-epics-and-stories"
        note: "Break down PRD into implementable epics with full context"
        completed: "2025-12-04"

      - id: "test-design"
        status: "skipped"
        agent: "tea"
        command: "/bmad:bmm:agents:tea then test-design"
        output: "System-level testability review"
        note: "Testing covered within Story 10"

      - id: "validate-architecture"
        status: "skipped"
        agent: "architect"
        command: "/bmad:bmm:agents:architect then validate-architecture"

      - id: "implementation-readiness"
        status: "docs/implementation-readiness-report-2025-12-04.md"
        agent: "architect"
        command: "/bmad:bmm:workflows:implementation-readiness"
        note: "Gate check before implementation"
        completed: "2025-12-04"

  phase_3_implementation:
    name: "Implementation"
    workflows:
      - id: "sprint-planning"
        status: "docs/sprint-artifacts/sprint-status.yaml"
        agent: "sm"
        command: "/bmad:bmm:workflows:sprint-planning"
        note: "Creates sprint plan - subsequent work tracked in sprint-status.yaml"
        completed: "2025-12-04"

      - id: "create-story"
        status: "required"
        agent: "sm"
        command: "/bmad:bmm:workflows:create-story"
        note: "Draft next story from epics - repeat for each story"

      - id: "dev-story"
        status: "required"
        agent: "dev"
        command: "/bmad:bmm:workflows:dev-story"
        note: "Implement story - repeat for each story"

      - id: "code-review"
        status: "required"
        agent: "dev"
        command: "/bmad:bmm:workflows:code-review"
        note: "Review implementation - repeat for each story"

      - id: "retrospective"
        status: "optional"
        agent: "sm"
        command: "/bmad:bmm:workflows:retrospective"
        note: "Run after epic completion"
